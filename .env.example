# Google Identity Services web client id (Google Cloud Console → Credentials → OAuth client, type "Web application",
# authorised JavaScript origin https://www.binggbongg.com). The SAME id must be appended to the backend's
# GOOGLE_SIGNIN_CLIENT_IDS so registration accepts the ID token.
VITE_GOOGLE_CLIENT_ID=
